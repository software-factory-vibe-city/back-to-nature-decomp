# Deprecated plans

Completed, executed, or superseded plans, kept for institutional record.
Each file's own status header states its outcome. Path references inside
these documents predate the move (they cite `plans/<name>.md`); resolve
them against this directory first, then the live `plans/` directory.
