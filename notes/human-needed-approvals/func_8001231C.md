# func_8001231C — human decision needed

- **Parked:** 2026-08-11
- **Reason:** `asm-needs-human-approval`
- **Escalation reached:** none — parked by hand, before any attempt
- **Source:** `src/func_8001231C.c` (INCLUDE_ASM, untouched)

> This note was written by hand, not generated by `/auto_decompilation_loop`.
> The loop's usual prose for this reason ("the top escalation tier proposed a
> forbidden construct") does not apply: no tier ran. `asm-needs-human-approval`
> is the closest value in the `ParkReason` union to what is actually true, which
> is described below.

## What the loop needs

A policy decision, in advance, that the escalation ladder cannot make on its own.

This is the only function in the live undecompiled backlog (104 functions in
`build/callGraph.json`) that trips the register-saturation signature:

```
distinct allocatable registers >= 23  AND  max base-copy fan-out >= 5
   measured: regs 25, fan-out 13, 19 base-address copies, 159 instructions
```

Its twin `func_80012598` is the same carve routine for a different
configuration — same `0x40` frame, same `memset(0x801BE1B0, 0, 0x3EE50)`, and a
first loop whose 21 register webs and store partition are *identical*. That
function was matched on 2026-08-11 and **required a user-granted `register-asm`
+ `embedded-asm` exception** (allowlisted in `.pi/autodecomp.json`), because the
target's base-address copies are reload rematerialisations with one-instruction
live ranges that no clean-C spelling currently reproduces. The best clean-C
state reached there was 55 differing words at the exact instruction count.

So the expected outcome here is the same exception. Deciding that up front is
cheaper than letting the ladder discover it: the first session on `func_80012598`
ended `escalation-exhausted` at 61 differing words having burned every tier.

## The decision

**Option A — grant the exception up front.** Add
`"func_8001231c": ["register-asm", "embedded-asm"]` to
`.pi/autodecomp.json` under `sourcePolicy.allowlist`, unpark, and run a directed
session against the recipe in `notes/research/func_8001231C.md`. Expected to be
short: six deltas from `src/func_80012598.c`, and the second phase is a
`jal func_8001E160` to an already-matched function rather than an inlined loop.

**Option B — hold it as clean-C debt.** Leave it parked until someone finds a
single-set, per-view-distinct C spelling of a base-equal copy — the one open
problem named in `notes/research/func_80012598.md` §4.6. Solving it would let the
exception be removed from `func_80012598` as well, so the payoff is two
functions, not one.

Do not grant the allowlist entry until the function actually matches: an
allowlist entry asserts, permanently and to every later agent, that the exempted
construct is the correct answer for that function, and being stuck is not that
assertion.

## Policy findings

- none recorded (nothing was attempted)

## Last oracle report

Not attempted. `psx_diff_function func_8001231C` currently reports MATCH only
because the body is still `INCLUDE_ASM`.

## References

- `notes/research/func_8001231C.md` — the full recipe: the six deltas, the
  register-rotation table, verification commands, traps, refuted hypotheses,
  and a stop rule
- `notes/research/func_80012598.md` — the mechanism (§3.1), the web partition
  (§3.2), the residual and refutations (§4), and the policy exception (§6)
- `src/func_80012598.c` — the source to copy
- `src/func_8001E160.c` — the matched function this one calls
- `notes/file-groupings.md` — boot/main-loop TU, render-context sub-family
